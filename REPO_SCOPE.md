# ขอบเขตคลัง Zero

คลังนี้เก็บซอร์สโค้ด แบบทดสอบ เอกสาร แผนงาน และค่าตัวอย่างของ Zero-ChatGPT
โดยทำงานจากโฟลเดอร์ต้นฉบับโดยตรง ไม่ใช้วิธีคัดลอกไฟล์ไปยังโฟลเดอร์สำหรับอัปโหลด

## สิ่งที่เก็บใน Git

- `src/` — ซอร์สโค้ดหลัก
- `test/` และ `test-fixtures/` — แบบทดสอบและข้อมูลทดสอบที่ไม่เป็นความลับ
- `config/` — ค่าตัวอย่างที่ปลอดภัยสำหรับเผยแพร่
- `docs/`, `Plan/`, `reports/` — เอกสาร แผน และรายงานที่ตั้งใจเก็บถาวร
- `example-scb-cop/` — ตัวอย่างประกอบโครงการ
- `vendor/` — ซอร์สจากภายนอกที่โครงการต้องใช้ โดยไม่รวมผลคอมไพล์
- `README.md`, `package.json` และไฟล์กำกับคลัง

## สิ่งที่ไม่ส่งขึ้น GitHub

- `runtime/` — โทเคน คุกกี้ สถานะเซสชัน โปรไฟล์เบราว์เซอร์ และผลตรวจสด
- `.zero/` — สถานะงาน บันทึกชั่วคราว และข้อมูลสำรองภายในเครื่อง
- `node_modules/`, `dist/`, `build/`, `coverage/`, `bin/`, `obj/` — สิ่งที่ติดตั้งหรือสร้างใหม่ได้
- ไฟล์ความลับ ใบรับรอง ฐานข้อมูล บันทึกเหตุการณ์ และไฟล์ชั่วคราว

## ขั้นตอนก่อนอัปโหลด

1. ตรวจ `git status` และรายชื่อไฟล์ที่เตรียมบันทึก
2. ตรวจหาโทเคน คุกกี้ รหัสผ่าน กุญแจ และไฟล์ขนาดใหญ่
3. รันแบบทดสอบของโครงการ
4. บันทึกการเปลี่ยนแปลง แล้วส่งขึ้นคลังส่วนตัว `miru-zero/Zero`

ห้ามย้ายข้อมูลจาก `runtime/` หรือ `.zero/` เข้ามาในโฟลเดอร์ที่ติดตามเพื่อหลบกติกานี้
